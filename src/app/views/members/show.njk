{% extends "layout.njk" %}
{% block content %}
  <div class="card">

    <section class="avatar" style="background: url({{member.avatar_url}}) no-repeat"></section>

    <section class="details">
      <h3>Detalhes</h3>
      <div class="item">
        <div>Membro</div>
        <div>{{member.name}}</div>
      </div>

      <div class="item">
        <div>Idade</div>
        <div>{{member.age}}</div>
      </div>

      <div class="item">
        <div>Nascimento</div>
        <div>{{member.birth}}</div>
      </div>

      <div class="item">
        <div>Sexo</div>
        <div>{{"Masculino" if member.gender == "M" or "m" else "Feminino"}}</div>
      </div>

      <div class="item">
        <div>Blood</div>
        <div>{{member.blood}}</div>
      </div>

      <div class="item">
        <div>Altura</div>
        <div>{{member.height}}</div>
      </div>

      <div class="item">
        <div>Peso</div>
        <div>{{member.weight}}</div>
      </div>

      <a href="/members/{{member.id}}/edit" class="button">Editar</a>
    </section>
  </div>
{% endblock content %}